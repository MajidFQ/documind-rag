"""
backend package — DocuMind RAG pipeline components.

This empty __init__.py makes the backend directory a proper Python package,
allowing relative imports like `from .config import X` to work correctly
regardless of where the application is launched from.
"""
